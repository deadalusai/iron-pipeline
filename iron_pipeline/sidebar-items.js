initSidebarItems({"enum":[["Error","Errors which may be raised by the Pipeline itself"]],"mod":[["middleware","Middleware for use in a `Pipeline`."],["prelude","Includes the Pipeline type and all middleware types in the `middleware` module."]],"struct":[["Pipeline","Iron middleware for implementing a simple forward-only pipeline. When a request is received each middleware is invoked in the order in which they were registered."],["PipelineNext","Handle used to invoke the next handler in a pipeline"]],"trait":[["Middleware","Trait which defines middleware within a pipeline. Implementors of this trait must call `next.handle(...)` in order to pass control to the next middleware in the pipeline."]]});